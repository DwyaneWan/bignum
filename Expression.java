package P1_bignum;

/**
 * Created by dwyan on 10/3/2016.
 */
public class Expression {
    String exp;
    int order;

    Expression(){
        order = 0;
        exp = "";
    }
//exp stores the expressions in correct order, order indicates the current priority of the expression. 0 means +, 1 means * and 2 means ^
}

